# 在 iPhone 上用這個 App

整個流程約 15 分鐘，全部免費，不用上架 App Store。

原理：把程式放到網路上拿到一個網址，iPhone 用 Safari 打開後「加入主畫面」，就變成一個真正的 App 圖示——**沒有網址列、沒有分頁、可以離線用**。

> 建議順序：先做這份（讓手機能用），再做 **`雲端同步設定教學.md`**（讓兩邊資料自動一致）。

---

## 第 1 步：安裝 Node.js（一次就好，3 分鐘）

這是上傳工具需要的東西，裝完就不用再管它。

1. 打開 **https://nodejs.org**
2. 點綠色的 **LTS** 版本下載
3. 安裝時全部按「下一步」，不用改任何設定
4. **裝完重新開機**（或至少把所有視窗關掉）

---

## 第 2 步：第一次上傳（5 分鐘）

雙擊資料夾裡的 **`手機版-第一次設定.bat`**。

它會自動幫你做四件事，中間需要你操作的只有兩個地方：

**① 登入 Netlify**
瀏覽器會自動打開 → 選 **Sign up with Google** → 按 **Authorize**
（Netlify 是免費的網頁空間，你的 App 會放在這裡）

**② 回到黑色視窗，回答三個問題**

| 問題 | 怎麼選 |
|---|---|
| What would you like to do? | 用方向鍵選 **`[+] Create & configure a new site`** 按 Enter |
| Team | 直接按 Enter |
| Site name | 打一個好記的英文名，例如 `jerry-daily`，按 Enter |

跑完之後畫面會出現：

```
Website URL:  https://jerry-daily.netlify.app
```

**這一行就是你的 App 網址。** 複製起來。

---

## 第 3 步：把網址存下來

打開資料夾裡的 **`網址.txt`**，把裡面的 `https://` 換成你剛拿到的完整網址，存檔。

這樣做的好處：以後桌面捷徑會直接開這個網址版本，電腦和手機看的是同一份程式。

---

## 第 4 步：iPhone 加到主畫面（2 分鐘）

**一定要用 Safari**，用 Chrome 或 LINE 內建瀏覽器裝不成 App。

1. iPhone 打開 **Safari**，輸入你的網址
   （懶得打的話：用 LINE 傳給自己，然後**長按連結 → 在 Safari 開啟**）
2. 確認畫面正常顯示
3. 點畫面**最下方正中間的「分享」按鈕**（正方形往上一支箭頭 ⬆️）
4. 選單往下滑，找到 **「加入主畫面」**
5. 名稱維持「每日任務」，點右上角 **「加入」**
6. 回主畫面 → 出現深色圓角圖示 → 點開

打開後應該是**全螢幕、沒有網址列**。這樣就成功了。

> 之後請**只用主畫面的圖示開**，不要再用 Safari 開網址。兩邊資料是分開存的，混用會看到兩份不同紀錄。

---

## 第 5 步：確認離線可以用

裝好後開飛航模式，點主畫面圖示，應該還是正常開啟、正常勾選。

（如果打不開，先在有網路時多開幾次，讓它把檔案存下來。）

---

## 第 6 步：接上雲端同步

接著做 **`雲端同步設定教學.md`**，做完之後電腦和手機的資料就會自動一致，不用再手動搬。

如果你**暫時不想做同步**，可以先用手動的方式把電腦資料搬過去：

1. 電腦 App → 設定 → **⬇ 匯出備份**（會下載一個 JSON）
2. 用 **AirDrop** 傳到 iPhone（或 LINE 傳給自己 → 儲存到「檔案」）
3. iPhone App → 設定 → **⬆ 匯入備份** → 選那個檔案 → 確定

---

## 之後我改了程式，怎麼更新手機

**雙擊 `手機版-一鍵更新.bat`，等 10 秒，結束。**

然後手機把 App 完全滑掉（往上滑關閉）再重開，就是新版了。

不用開瀏覽器、不用拖資料夾、不用重新加到主畫面。

---

## 常見問題

**加到主畫面後打開還是有網址列**
是用 Chrome 加的。刪掉圖示，改用 Safari 重做第 4 步。

**圖示變成網頁截圖，不是 App 圖示**
清一下 Safari 快取（設定 → Safari → 清除瀏覽紀錄與網站資料 ⚠️ 這會清掉還沒同步的紀錄，先匯出備份），再重新加一次。

**`.bat` 檔一閃就關掉**
代表出錯了。在檔案上按右鍵 → 用「系統管理員身分執行」再試一次；還是不行就截圖給我。

**`.bat` 說找不到 Node.js，可是我裝了**
需要重新開機讓系統認得到。重開後再試。

**上傳失敗，說登入過期**
按 Windows 鍵打 `cmd` 開啟命令提示字元，輸入 `netlify login` 按 Enter，重新授權一次。

**這個網址別人會看到嗎**
網址不會被搜尋引擎收錄。就算有人知道網址，打開也只會看到一個**空的 App**——你的紀錄存在你自己的裝置（或你的 Supabase 帳號底下），別人拿不到。
另外，上傳的只有程式檔案，`backups` 資料夾和這些說明文件都不會被上傳。

**手機上排版怪怪的**
截圖傳給我，我調。
