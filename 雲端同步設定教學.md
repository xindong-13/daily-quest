# 雲端同步設定教學

做完之後：**電腦改、手機馬上有；手機改、電腦馬上有。** 換手機、清瀏覽器資料都不會掉。

用 Supabase 的免費方案，你的資料量小到永遠碰不到上限。全程免費，不需要信用卡。

約 15 分鐘。照著做就好，不用懂程式。

---

## 第 1 步：註冊 Supabase（3 分鐘）

1. 電腦打開 **https://supabase.com**
2. 右上角 **Start your project**
3. 選 **Continue with GitHub** 或 **Continue with Google**（用 Google 最快）
4. 授權登入

---

## 第 2 步：建立專案（3 分鐘）

1. 登入後點 **New project**
2. 填三個欄位：
   - **Name**：`daily-quest`（隨便打，自己看的）
   - **Database Password**：按 **Generate a password**，然後**點旁邊的複製圖示存起來**
     （這是資料庫密碼，跟等下 App 裡要打的密碼是兩回事。存著以防萬一，正常用不到）
   - **Region**：選 **Northeast Asia (Tokyo)** 或 **Southeast Asia (Singapore)**，離台灣近比較快
3. 點 **Create new project**
4. 等 1～2 分鐘讓它建好（畫面會顯示 Setting up project）

---

## 第 3 步：建立資料表（2 分鐘）

1. 左側選單找到 **SQL Editor**（圖示像一個資料庫加上 `>_`）
2. 點 **New query**
3. **把下面整段完整複製貼上**：

```sql
create table if not exists public.app_state (
  user_id    uuid primary key references auth.users(id) on delete cascade,
  data       jsonb not null,
  updated_at timestamptz not null default now()
);

alter table public.app_state enable row level security;

drop policy if exists "own row read"   on public.app_state;
drop policy if exists "own row insert" on public.app_state;
drop policy if exists "own row update" on public.app_state;

create policy "own row read"   on public.app_state
  for select using (auth.uid() = user_id);

create policy "own row insert" on public.app_state
  for insert with check (auth.uid() = user_id);

create policy "own row update" on public.app_state
  for update using (auth.uid() = user_id) with check (auth.uid() = user_id);
```

4. 點右下角 **Run**（或按 `Ctrl + Enter`）
5. 看到 **Success. No rows returned** 就成功了

> 這段的意思：建一張表存你的資料，並且設定「只有你本人能讀寫自己那一列」。
> 就算別人拿到你的網址和公開金鑰，也讀不到你的東西。

---

## 第 4 步：關掉信箱驗證（1 分鐘）

不關的話註冊完要去收信點連結，很麻煩。

1. 左側 **Authentication** → 上方 **Sign In / Providers**（有些版本叫 Providers）
2. 找到 **Email**，點進去
3. 把 **Confirm email** 的開關**關掉**
4. 點 **Save**

> 找不到的話：左側 **Authentication** → **Emails** 或 **Settings**，找 "Confirm email" / "Enable email confirmations" 這個開關關掉即可。

---

## 第 5 步：拿兩把鑰匙（1 分鐘）

1. 左側最下方 **Project Settings**（齒輪）→ **API Keys** 或 **API**
2. 複製這兩個值，先貼到記事本：

| 名稱 | 長相 |
|---|---|
| **Project URL** | `https://abcdefghijk.supabase.co` |
| **anon public** | `eyJhbGciOiJIUzI1NiIs...` 很長一串 |

> ⚠️ 只要 **anon / public** 那一把。**service_role 那把絕對不要用、不要貼給任何人**，它可以繞過所有權限。

---

## 第 6 步：在電腦上連起來（2 分鐘）

1. 打開你的每日任務 App → 右下角 **⚙️ 設定**
2. 找到 **☁️ 雲端同步**
3. 把 **Project URL** 和 **anon public key** 貼進去 → **儲存設定**
4. 畫面會變成登入表單。這裡的帳號密碼是**你自己現在決定的**，跟 Supabase 帳號無關：
   - **電子信箱**：填你的信箱（例如 jerry051194@gmail.com）
   - **密碼**：自己想一組，**至少 6 個字**，記起來，等下手機要用同一組
5. 點 **第一次：註冊**
6. 成功的話狀態列會變成綠色「已同步」

### 如果跳出「兩邊都有資料」

第一次連線時，如果雲端和這台都有東西，App 會問你要留哪一份。
第一次設定的時候雲端是空的，通常不會出現。真的出現就選 **用這台這份**。

---

## 第 7 步：在手機上連起來（2 分鐘）

1. iPhone 打開主畫面的 **每日任務**
2. **⚙️ 設定** → **☁️ 雲端同步**
3. 貼上**一模一樣的** Project URL 和 anon key
   （用 LINE 傳給自己再複製貼上最快）
4. 用**同一組信箱和密碼**，這次點 **登入**（不是註冊）
5. 會跳出「兩邊都有資料」→ 選 **用雲端這份**（電腦那份才是完整的）

完成。

---

## 確認有成功

1. 電腦上新增一件事，例如「測試同步」
2. 手機把 App 滑掉重開（或切出去再切回來）
3. 手機應該就看到「測試同步」

反過來再試一次：手機勾掉它，電腦切出去再切回來，應該也會變成已完成。

---

## 之後怎麼運作

- **改任何東西**（新增、勾選、記目標進度）→ 約 1.5 秒後自動上傳
- **App 從背景切回前景** → 自動抓最新的
- **每 2 分鐘**在前景時也會自動檢查一次
- **離線**（飛航模式、沒訊號）→ 照常使用，狀態列顯示「離線」，恢復連線後自動補上
- 設定頁隨時可以按 **立即同步** 手動觸發

### 兩邊同時改會怎樣？

以「最後修改時間」為準，比較新的那一份會覆蓋另一份。

單人兩台裝置幾乎不會撞到。真的要小心的情境是：**手機離線改了東西、放著沒開；同時電腦又改了東西**——這時後開的那台會覆蓋掉前面的。習慣上「用完一台就切出去讓它同步一次」就不會有問題。

### 資料還是要備份

雲端同步不等於備份——如果誤刪，兩邊會一起刪。建議還是每隔一陣子在設定頁按 **⬇ 匯出備份** 存一份。

---

## 建議：電腦也改用網址開

同步設定好之後，建議電腦也改用手機那個網址，兩邊完全一致。

做法：把手機版網址貼進資料夾裡的 **`網址.txt`**（取代原本的 `https://`），存檔。之後雙擊桌面捷徑就會開網址版，一樣沒有網址列。

想改回本機檔案版，把 `網址.txt` 裡的內容刪掉存檔就好。

> 為什麼建議這樣？因為「用檔案開」和「用網址開」在瀏覽器眼中是兩個不同的地方，資料是分開存的。統一用網址，腦袋比較不會打結。
> （另外，用檔案直接開啟時，瀏覽器有可能因為安全限制擋掉雲端連線；用網址就完全沒這問題。）

---

## 出問題的時候

**狀態顯示「同步失敗 401」**
anon key 貼錯或貼不完整。回第 5 步重新整串複製。

**狀態顯示「同步失敗 404」**
第 3 步的資料表沒建成功。回去重跑一次 SQL。

**註冊時說「需要先驗證信箱」**
第 4 步的 Confirm email 沒關掉。關掉之後再註冊一次。

**註冊時說 User already registered**
這個信箱註冊過了，改按 **登入**。

**手機看不到電腦的資料**
確認兩邊的 Project URL 和 anon key**完全一樣**（常見是複製時少了開頭或結尾），而且是**同一組帳號密碼**。

**忘記密碼**
到 Supabase 後台 **Authentication → Users**，找到你的信箱，右邊的選單可以直接改密碼。

其他狀況把設定頁的錯誤訊息截圖給我，我看得出問題在哪。
