# 雲端同步設定教學（同步代碼版）

**不需要帳號密碼。** 產生一組同步代碼，兩台裝置填同一組就會同步。
跟你另外兩個 App（コトバ Kotoba、Echo 英語）用的是同一套做法。

做完之後：電腦改、手機馬上有；手機改、電腦馬上有。

---

## 三個 App 會不會打架？不會

| App | 用哪張表 | 認人方式 |
|---|---|---|
| コトバ Kotoba | `kotoba_sync` | 同步代碼 |
| Echo 英語 | `echo_sync` | 同步代碼 |
| **每日任務** | **`daily_sync`** | **同步代碼** |

三張表互相獨立，可以放在**同一個 Supabase 專案**裡，完全不會互相影響。
你已經有專案的話，直接用同一個就好，不用再開一個。

---

## 已經有 Supabase 專案 → 從步驟 3 開始

## 步驟 1｜註冊 Supabase（3 分鐘）

1. 打開 **https://supabase.com** → 右上角 **Start your project**
2. 選 **Continue with Google** → 授權

## 步驟 2｜建立專案（3 分鐘）

1. **New project**
2. 填三個欄位：
   - **Name**：`my-apps`（隨便打）
   - **Database Password**：按 **Generate a password**，複製起來存著（正常用不到）
   - **Region**：選 **Northeast Asia (Tokyo)**
3. **Create new project** → 等 1～2 分鐘

> ⚠️ **免費專案閒置一週會自動暫停。** 暫停不會刪資料，到 Dashboard 按
> **Restore project** 就會回來。平常有在用就不會被暫停。

## 步驟 3｜建立資料表（2 分鐘）

**最簡單的做法：讓 App 幫你把語法產生好。**

1. 打開你的每日任務 App → **⚙️ 設定** → **☁️ 雲端同步**
2. 點 **📋 顯示要貼到 Supabase 的建表語法** → **複製語法**
3. 打開 supabase.com → 你的專案
4. 左邊選單找 **SQL Editor**（終端機圖示 `>_`）→ **New query**
5. 編輯區**全部清空**（Ctrl+A → Delete）→ 貼上 → 右上角綠色 **Run**
6. 看到 **Success** 就完成了

> 出現 `Backend error! Retry your query` → 那是 Supabase 自己的小問題，**再按一次 Run**。
> 一直失敗 → 去 Dashboard 看專案是不是 **PAUSED**，是的話先 Restore。

## 步驟 4｜拿兩把鑰匙（1 分鐘）

Supabase 左下角 **Project Settings**（齒輪）→ **API Keys**（或 **API**）：

| 名稱 | 長相 |
|---|---|
| **Project URL** | `https://abcdefghijk.supabase.co` |
| **anon** **public** | `eyJhbGciOiJIUzI1NiIs...` 很長一串 |

> ⚠️ 只拿 **anon / public** 那一把。**`service_role` 絕對不要用、不要外流。**

📱 順手用 LINE 傳給自己，等一下手機要用。

## 步驟 5｜電腦連線（2 分鐘）

App → **⚙️ 設定** → **☁️ 雲端同步**：

1. 貼上 **Project URL**
2. 貼上 **anon public key**
3. 點 **🎲 產生新代碼** → 會出現像 `DQ-K7M2-P9XR-T4WN` 的一串
4. 點 **連線**

連線時會自動測試網址、金鑰、資料表都對不對，有問題會直接告訴你哪裡錯。

✅ 成功後畫面會把代碼框起來顯示，點 **📋 複製代碼** 存到備忘錄。

## 步驟 6｜手機連線（2 分鐘）

手機打開 App → **⚙️ 設定** → **☁️ 雲端同步**：

1. 貼上**一模一樣的** Project URL 和 anon key
2. **同步代碼填電腦那一組**（不要按產生新的！）
3. 點 **連線**
4. 跳出「兩邊都有資料」→ 選 **☁️ 用雲端這份**

完成。

---

## 確認有成功

1. 電腦新增一件事，叫「測試同步」
2. 手機切出去再切回來
3. 應該就看到了

反過來再試一次：手機把它勾掉，電腦切走再切回來，應該變成已完成。

測完把「測試同步」刪掉。

---

## 平常怎麼運作

| 情況 | 會發生什麼 |
|---|---|
| 新增、勾選、記進度 | 約 0.9 秒後自動上傳 |
| 切走／關掉視窗 | 馬上把還沒送的送出去 |
| 切回來／視窗取得焦點 | 立刻抓最新的 |
| 前景放著 | 每 20 秒檢查一次 |
| 離線 | 照常用，恢復連線自動補送 |
| 手動觸發 | 點主畫面的 **☁️ 標籤**，或設定頁 **🔄 立即同步** |

**兩邊同時改會自動合併**——電腦新增的任務、手機勾掉的紀錄都會保留，不會互相覆蓋。

---

## 安全性：老實說

同步代碼這種做法，**代碼就是唯一的鑰匙**。
理論上，如果有人同時知道你的 Project URL、anon key、**還有你的代碼**，就能讀寫你的資料。

實際上要擔心的程度很低：

- 這三樣東西都只存在你自己的裝置裡，**不在公開的 GitHub repo 裡**
- 代碼是 12 位隨機字元（約 60 bits），猜不到

**你要做的只有一件事：不要把代碼貼到公開的地方。**

（之前的信箱＋密碼版本安全性更高，但你要記密碼、還可能被鎖在外面——就是這次遇到的狀況。
兩者取捨過後選了這個。）

---

## 出問題的時候

| 狀況 | 怎麼辦 |
|---|---|
| 連線說 **anon key 不對** | 回步驟 4 整串重新複製，注意開頭 `eyJ` 和結尾都要完整 |
| 連線說 **找不到資料表** | 步驟 3 的 SQL 沒跑成功，回去重跑一次 |
| SQL Editor 一直 Backend error | 先按重試；還是不行看 Dashboard 專案是否 **PAUSED** → Restore |
| 手機看不到電腦的資料 | 三樣東西要**完全一樣**：Project URL、anon key、同步代碼 |
| 代碼忘了 | 電腦上還連著的話：設定 → 雲端同步 → 上面就顯示著，按複製 |
| 兩邊怎樣都對不起來 | 在資料最完整的那台按 **⬆ 以這台為準**，另一台按 **🔄 立即同步** |

**最重要的一點**：資料同時存在你的裝置本機，雲端只是橋樑。
真的出事就用 **設定 → 📋 複製成文字 / 📥 從文字還原** 手動搬，一定成功。
