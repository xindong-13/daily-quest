# 每日任務 — 從頭到尾完整設定教學

**這一份包含所有步驟。** 資料夾裡的 `手機安裝教學_iPhone.md` 和 `雲端同步設定教學.md` 是這份的拆解版，內容一樣，你照這份做就好。

---

> ## 📌 你現在的進度與剩下的六步
>
> **已完成：** Node.js ✓　Netlify 站台 `https://jerry-daily.netlify.app` ✓　`site-url.txt` ✓
> **手機資料已備份 ✓**（16 件事、4 個目標、3 天紀錄，已存進 `backups` 資料夾）
>
> ### 接下來照這個順序做，資料不會掉
>
> | # | 在哪 | 做什麼 | 時間 |
> |---|---|---|---|
> | **1** | 電腦 | 雙擊 `手機版-一鍵更新.bat`，把最新版推上 jerry-daily | 1 分 |
> | **2** | 電腦 | 開 `https://jerry-daily.netlify.app` → 設定 → **⬆ 匯入檔案** → 選你的備份 JSON | 2 分 |
> | **3** | 電腦 | 建 Supabase（【階段 D】） | 8 分 |
> | **4** | 電腦 | App 設定頁連上同步、**註冊**（【階段 E】）→ 資料上雲端 | 3 分 |
> | **5** | 手機 | 刪掉舊圖示，Safari 開 jerry-daily → 加入主畫面（【階段 F】） | 2 分 |
> | **6** | 手機 | 設定頁用**同一組帳密登入** → 選「☁️ 用雲端這份」（【階段 G】） | 2 分 |
>
> 最後做【階段 H】驗證。
>
> ### ⚠️ 三個一定要注意的點
>
> 1. **第 5 步刪舊圖示之前，先確認第 4 步電腦上的狀態列是綠色「已同步」。** 資料進了雲端才動手機。
> 2. **手機那邊是按「登入」不是「註冊」**，而且要選 **「用雲端這份」**（不是「用這台這份」，不然會用手機的空資料覆蓋掉雲端）。
> 3. 備份 JSON 已經存在 `backups` 資料夾，**任何一步出錯都可以用它救回來**，不用怕。
>
> ---
>
> ### 為什麼要換網址、資料會不會不見
>
> 你之前拖資料夾產生的是另一個網址，手機的資料就存在那個網址底下。瀏覽器是**依網址分隔資料**的，所以直接開 jerry-daily 會看到空的。
>
> 解法就是上面的流程：**先把備份匯入電腦 → 推上雲端 → 手機從雲端拉回來**。繞一圈之後兩邊都是同一份，以後就再也不用搬了。
>
> ---
>
> ### 關於 `.bat` 之前跑出一堆亂碼錯誤
>
> 那是我的問題，已修好——`.bat` 裡的中文在你的命令視窗編碼下爛掉，導致每一行被當成指令執行。現在所有 `.bat` / `.vbs` 內容都是純英文，中文說明留在這份文件裡。

---

## 做完之後會變成什麼樣

- iPhone 主畫面有一個 App 圖示，點開全螢幕、沒有網址列、離線也能用
- 電腦和手機**資料自動一致**：電腦勾一件事，手機切出去再切回來就看到
- 我改程式之後，你**雙擊一個檔案、等 10 秒**，手機就更新了

**總共約 30 分鐘。全部免費，不需要信用卡，不用上架 App Store。**

---

## 先搞懂三個地方（很重要，看懂後面就不會迷路）

```
  ┌─────────────────────────┐
  │  你桌面的資料夾          │   ← 程式的「原始檔」，我改的就是這裡
  │  claude for daily app   │
  └───────────┬─────────────┘
              │  你雙擊 .bat 上傳
              ▼
  ┌─────────────────────────┐
  │  Netlify                │   ← 放「程式」的地方，會給你一個網址
  │  (免費網頁空間)          │
  └───────────┬─────────────┘
              │  電腦和手機都從這裡讀程式
      ┌───────┴────────┐
      ▼                ▼
  ┌────────┐      ┌────────┐
  │ 電腦    │      │ iPhone │
  └───┬────┘      └───┬────┘
      │                │
      └───────┬────────┘
              ▼  讀寫「資料」
  ┌─────────────────────────┐
  │  Supabase               │   ← 放「你的任務紀錄」的地方
  │  (免費資料庫)            │
  └─────────────────────────┘
```

**兩件事分開記：**

| | 放在哪 | 什麼時候更新 |
|---|---|---|
| **程式**（畫面、功能） | Netlify | 我改東西時，你手動推一次 |
| **資料**（你的任務紀錄） | Supabase | 隨時自動，你不用管 |

---

## 要傳到 Netlify 的到底是什麼

**會上傳的（6 項）：**

```
index.html        主畫面
app.js            所有程式邏輯
style.css         外觀樣式
manifest.json     App 名稱、圖示設定
sw.js             離線功能
icons/            App 圖示（整個資料夾）
```

**不會上傳的（也不該上傳）：**

```
backups/                  你的備份檔（含個人紀錄，絕對不能公開）
*.md                      所有說明文件
*.bat / *.vbs             電腦用的啟動與上傳工具
網址.txt
.netlify/ .deploy/        工具自動產生的
```

> **你不需要手動挑檔案。** `手機版-一鍵更新.bat` 會自動把該傳的複製到一個暫存資料夾再上傳，備份和說明文件永遠不會被傳出去。

---

## 網址從哪裡來

⚠️ **我沒辦法先給你網址。**

網址是你在【階段 B】按下上傳時，由 Netlify 當場產生的，而且名字是你自己取的。我沒有你的 Netlify 帳號，沒辦法代你建立。

它會長這樣：

```
https://jerry-daily.netlify.app
        └──────┬──────┘
          你自己取的名字
```

【階段 B】跑完，黑色視窗裡會出現 `Website URL:` 那一行，**那就是你的網址**。

---
---

# 階段 A｜電腦：安裝 Node.js

**目的**：上傳工具需要它。裝一次，以後不用再管。

1. 打開 **https://nodejs.org**
2. 點綠色的 **LTS** 那顆按鈕下載（不要選 Current）
3. 執行下載的安裝檔，全部按 **Next / 下一步**，不用改任何設定
4. **裝完重新開機**

> 為什麼要重開機：Windows 要重新載入環境變數，不重開的話下一步會說「找不到 Node.js」。

---

# 階段 B｜電腦：第一次上傳，拿到網址

**目的**：把程式放上網，拿到你的專屬網址。

## B-1　雙擊 `手機版-第一次設定.bat`

在桌面的 `claude for daily app` 資料夾裡。

會跳出一個黑色視窗。如果 Windows 跳出「已保護您的電腦」，點 **其他資訊 → 仍要執行**。

## B-2　等它自動裝工具

視窗是英文的，對照如下：

| 螢幕顯示 | 意思 |
|---|---|
| `[1/4] Node.js found:` | Node.js 找到了 ✓ |
| `[2/4] Installing upload tool (Netlify CLI)...` | 正在裝上傳工具 |
| `First time takes 1-2 minutes. Please wait.` | 第一次要等 1-2 分鐘，請等 |

**這一步要等 1～2 分鐘，看起來像卡住是正常的**，不要關掉。

## B-3　登入 Netlify

畫面顯示 `[3/4] Login to Netlify` 後，瀏覽器會自動打開。

1. 點 **Sign up with Google**（或 GitHub）
2. 選你的 Google 帳號
3. 出現授權頁面 → 點 **Authorize**
4. 看到 "You are now logged in" 之後，**回到黑色視窗**

> Netlify 是放網頁的免費空間，你的方案完全免費，不會扣款。

## B-4　回答三個問題

視窗會顯示 `Press any key to continue`，按一下任意鍵，然後問你三個問題。

**問題 1**
```
? What would you like to do?
  ❯ [+]  Create & configure a new site
    [<->] Link this directory to an existing site
```

用**上下方向鍵**選，按 **Enter**：

| 你的情況 | 選哪個 |
|---|---|
| **之前已經拖過資料夾上去、手機已經能用**（← **你是這個**） | **`Link this directory to an existing site`** |
| 從來沒上傳過 | `[+] Create & configure a new site` |

> **選 Link 的話**，接著會列出你的站台清單，用方向鍵選你那一個（名字通常像 `sparkly-pastry-a1b2c3`），按 Enter。
> 這樣就會覆蓋你現有的網址，**手機不用重新加到主畫面**，直接就是新版。
>
> **不小心選了 Create（建新的）也沒關係**，只是會拿到一個全新的網址。跑完之後：
> 1. 複製新網址貼進 `site-url.txt`
> 2. iPhone 長按舊圖示 → **移除 App**
> 3. 用 Safari 開新網址 → 分享 → 加入主畫面
>
> 之後固定用新的就好，舊站台不用管它。
>
> 新版的 Netlify CLI 會把 "site" 講成 "project"，意思一樣。

**問題 2**（只有選 Create 才會問）
```
? Team:
> 67king
```
→ **直接按 Enter**

> ⚠️ 這裡最容易以為當機。
> 前面那個 `>` 代表游標已經停在那一項上了，這是一個「選單」不是輸入框，**按 Enter 就是選它**。
> 畫面不會有任何提示叫你按 Enter，看起來就像卡住，但其實它在等你。
> 後面所有出現 `>` 或 `❯` 的畫面都一樣：方向鍵選、Enter 確認。

**問題 3**（只有選 Create 才會問）
```
? Site name (optional):
```
→ 打一個**好記的英文小寫名字**，例如 `jerry-daily`，按 **Enter**
（只能用英文字母、數字、減號。如果說名字被用過了，換一個，例如 `jerry-daily-2026`）

## B-5　複製你的網址

跑完會出現類似這樣：

```
Deploy is live!

Unique Deploy URL: https://68ab...--jerry-daily.netlify.app
Website URL:       https://jerry-daily.netlify.app
```

👉 **`Website URL:` 那一行就是你的網址。**（如果你剛剛選 Link，這就是你原本那個網址）

在黑色視窗裡用滑鼠**選取那段文字，按 Enter 就複製了**（Windows 命令視窗的複製方式）。

最後會顯示：

```
SUCCESS
  1. Copy it  (select the text, then press Enter)   ← 複製網址
  2. Paste it into  site-url.txt  in this folder    ← 貼進 site-url.txt
  3. Open it on your iPhone using SAFARI            ← 用 Safari 打開
```

**先在電腦瀏覽器貼上網址打開看看**，應該會看到你的 App，而且**設定頁最下面應該出現「☁️ 雲端同步」卡片**。看到它就代表新版推上去了。

### 😱 視窗關掉了、沒來得及複製網址？

網址不會不見，站台已經建好了。兩個方法：

**方法 1（推薦）**：雙擊資料夾裡的 **`顯示我的網址.bat`**
會印出你的網址，並自動開一個瀏覽器分頁到你的站台，從網址列複製最方便。

**方法 2**：打開 **https://app.netlify.com**，用你剛剛的 Google 帳號登入，站台清單裡就是你的網址。

> 順帶一提：黑色視窗會突然關掉，通常是因為在前面的選單狂按 Enter，多餘的按鍵被後面的「請按任意鍵繼續」吃掉了。下次選單選好按**一次**就好。

---

# 階段 C｜電腦：把網址存進 `site-url.txt`

**目的**：讓桌面捷徑也開網址版，這樣電腦和手機看的是同一份程式。

1. 打開資料夾裡的 **`site-url.txt`**（用記事本）
2. 把裡面的 `https://` 整行**換成你的完整網址**：
   ```
   https://jerry-daily.netlify.app
   ```
3. **存檔**（Ctrl + S）

之後雙擊桌面捷徑，就會開這個網址（一樣沒有網址列，看起來跟以前一模一樣）。

> 想改回本機檔案版：把 `site-url.txt` 內容全部刪光存檔就好。
>
> 資料夾裡原本那個 `網址.txt` 已經不用了，可以刪掉（捷徑兩個都會讀，留著也不會怎樣）。

---

## 📱 手機先確認一下

在做下一步之前，**手機把 App 完全滑掉再重開**（從螢幕底部往上滑並停住 → 把每日任務往上滑掉 → 重新點圖示）。

到 **⚙️ 設定**，應該要看到 **☁️ 雲端同步** 這張卡片。

- **看得到** → 新版推成功了，繼續下一步
- **看不到** → 手機還在用舊版快取。試試看：把 App 刪掉（長按圖示 → 移除 App），再用 Safari 開網址重新加入主畫面一次

---

# 階段 D｜電腦：建立 Supabase 資料庫

**目的**：資料要有地方存，電腦和手機才能共用。

## D-1　註冊

1. 打開 **https://supabase.com**
2. 右上角 **Start your project**
3. 選 **Continue with Google**
4. 授權登入

## D-2　建立專案

1. 點 **New project**
2. 填三個欄位：

| 欄位 | 怎麼填 |
|---|---|
| **Name** | `daily-quest`（隨便打，自己看的） |
| **Database Password** | 點 **Generate a password**，然後**點旁邊的複製圖示存到記事本** |
| **Region** | 選 **Northeast Asia (Tokyo)**（離台灣最近） |

> Database Password 跟等下 App 裡要打的密碼**是兩回事**。存起來以防萬一，正常用不到。

3. 點 **Create new project**
4. **等 1～2 分鐘**（畫面顯示 Setting up project）

## D-3　建立資料表

1. 左側選單點 **SQL Editor**
2. 點 **New query**
3. **把下面整段完整複製貼上**（從 `create` 到最後的分號）：

```sql
create table if not exists public.app_state (
  user_id    uuid primary key references auth.users(id) on delete cascade,
  data       jsonb not null,
  updated_at timestamptz not null default now()
);

alter table public.app_state enable row level security;

drop policy if exists "own row read"   on public.app_state;
drop policy if exists "own row insert" on public.app_state;
drop policy if exists "own row update" on public.app_state;

create policy "own row read"   on public.app_state
  for select using (auth.uid() = user_id);

create policy "own row insert" on public.app_state
  for insert with check (auth.uid() = user_id);

create policy "own row update" on public.app_state
  for update using (auth.uid() = user_id) with check (auth.uid() = user_id);
```

4. 點右下角 **Run**（或 `Ctrl + Enter`）
5. 看到 **Success. No rows returned** 就成功了

> 這段在做什麼：建一張表存你的資料，並且設定「只有你本人讀得到自己那一列」。就算有人拿到你的網址和公開金鑰，也讀不到你的東西。

## D-4　調整信箱登入設定

⚠️ **這一步最容易做錯**，因為那一頁有好幾個長得很像的開關。

1. 左側 **Authentication**
2. 上方分頁找 **Sign In / Providers**（有的版本叫 **Providers**）
3. 找到 **Email**，點進去
4. **對照下表，一個一個確認**：

| 開關名稱 | 應該設成 | 為什麼 |
|---|---|---|
| **Enable Email provider**（最上面那個總開關） | ✅ **開啟** | 關掉的話完全不能用信箱登入 |
| **Allow new users to sign up**（或 Enable sign ups） | ✅ **開啟** | 關掉的話沒辦法註冊新帳號 |
| **Confirm email**（或 Enable email confirmations） | ❌ **關閉** | 開著的話註冊完要去收信點連結，很麻煩 |

5. 點 **Save**，等 10 秒讓設定生效

> 🚨 **只要關「Confirm email」那一個就好，其他兩個要保持開啟。**
> 如果不小心把總開關關掉，App 會顯示 **「同步失敗：Email logins are disabled」**。回到這一頁把 Enable Email provider 打開就好。

## D-5　複製兩把鑰匙

1. 左側最下方 **Project Settings**（齒輪圖示）→ **API Keys**（或 **API**）
2. 複製這兩個，**貼到記事本存著**：

| 名稱 | 長相 |
|---|---|
| **Project URL** | `https://abcdefghijklm.supabase.co` |
| **anon** **public** | `eyJhbGciOiJIUzI1NiIsInR5cCI6...`（很長一串） |

> ⚠️ 只拿 **anon / public** 那一把。
> **`service_role` 那把絕對不要用、不要貼給任何人**，它可以繞過所有權限保護。

📱 **順手把這兩個用 LINE 傳給自己**，等一下手機要用。

---

# 階段 E｜電腦：App 連上同步

1. 打開你的每日任務 App（桌面捷徑，現在會開網址版）
2. 右下角 **⚙️ 設定**
3. 找到 **☁️ 雲端同步** 卡片
4. 貼上剛剛的兩個值：
   - **Project URL** → `https://xxxx.supabase.co`
   - **anon public key** → `eyJ...` 整串
5. 點 **儲存設定**

畫面會變成登入表單：

6. **電子信箱**：填你的信箱（例如 jerry051194@gmail.com）
7. **密碼**：**自己現在想一組**，至少 6 個字，**記起來，手機要用同一組**
   （這組密碼跟 Supabase 帳號、Database Password 都無關，是你這個 App 的專屬帳號）
8. 點 **第一次：註冊**

✅ 成功的話，狀態列會變成綠色 **「● 已同步」**

> 如果跳出「兩邊都有資料」的視窗（第一次通常不會）：選 **用這台這份**。

---

# 階段 F｜手機：加到主畫面

## F-1　用 Safari 打開網址

**一定要用 Safari。** 用 Chrome 或 LINE 內建瀏覽器裝不成 App。

最簡單的方式：
1. 用 LINE 把網址傳給自己
2. **長按那個連結 → 選「在 Safari 開啟」**

或直接在 Safari 網址列打 `jerry-daily.netlify.app`。

## F-2　加入主畫面

1. 確認畫面正常顯示 App
2. 點畫面**最下方正中間的「分享」按鈕**（⬆️ 正方形往上一支箭頭）
3. 選單**往下滑**，找到 **「加入主畫面」**
4. 名稱維持「每日任務」，點右上角 **「加入」**
5. 回到主畫面，出現一個深色圓角圖示

## F-3　確認

點開圖示，應該是**全螢幕、沒有網址列、沒有分頁列**。

> ⚠️ 之後請**只用主畫面的圖示開**，不要再用 Safari 開網址。
> 這兩個在瀏覽器眼中是不同的地方，資料是分開存的。

---

# 階段 G｜手機：連上同步

1. iPhone 打開主畫面的 **每日任務**
2. **⚙️ 設定** → **☁️ 雲端同步**
3. 貼上**和電腦一模一樣的** Project URL 和 anon key
   （從 LINE 複製最保險，手打容易錯字）
4. 點 **儲存設定**
5. 填**和電腦一模一樣的信箱和密碼**
6. 這次點 **登入**（**不是**註冊）

會跳出「**兩邊都有資料**」的視窗：

7. 選 **☁️ 用雲端這份**（電腦那份才是完整的）

✅ 狀態列變綠色，手機上就看得到電腦的所有紀錄了。

---

# 階段 H｜驗證有沒有成功

**測試 1：電腦 → 手機**
1. 電腦新增一件事，叫「測試同步」
2. 等 2 秒
3. 手機把 App 切出去（回主畫面）再切回來
4. 應該看到「測試同步」✅

**測試 2：手機 → 電腦**
1. 手機把「測試同步」打勾
2. 電腦切到別的視窗再切回 App
3. 應該變成已完成 ✅

**測試 3：離線**
1. 手機開飛航模式
2. 點主畫面圖示 → 應該正常打開、正常勾選
3. 狀態列顯示「離線」
4. 關掉飛航模式 → 自動補上同步 ✅

測完把「測試同步」刪掉就好。

---
---

# 日常使用

## 資料同步：什麼都不用做

| 情況 | 會發生什麼 |
|---|---|
| 新增、勾選、記目標進度 | 約 0.9 秒後自動上傳 |
| **切到別的 App／關掉視窗** | **馬上把還沒送的送出去**（就算當下關閉也會送完） |
| **切回這個 App／視窗重新取得焦點** | **立刻抓最新的** |
| 在前景放著 | 每 20 秒自動檢查一次 |
| 離線 | 照常使用，恢復連線後自動補送 |
| 想手動觸發 | 點主畫面的 **☁️ 標籤**，或設定頁按 **立即同步** |

主畫面人物卡上有一個 **☁️ 標籤**，顯示上次同步是多久前（剛剛／3 分前／離線／同步失敗）。**點它就會立刻同步**，不用跑到設定頁。

正常情況下你什麼都不用做——手機勾完切走，電腦點回來就看到了。

## 兩邊同時改會怎樣

**會自動合併，兩邊的東西都留得住。** 不是誰蓋掉誰。

| 你改了什麼 | 合併方式 |
|---|---|
| 新增／編輯任務 | 以任務為單位聯集，電腦新增的、手機新增的都會在 |
| 刪除任務 | 任一邊刪掉就是刪掉，不會被另一邊救回來 |
| 勾選／取消勾選 | **一天一天比**，那天以最後被改動的那一邊為準 |
| 長期目標的進度 | 每一筆進度各自獨立，兩邊記的都會留著 |
| 每天的手動順序 | 聯集 |
| 累積完成數、全清天數 | 合併後**從紀錄重新計算**，不會重複累加 |

所以「電腦昨晚新增任務」＋「手機早上勾了東西」→ 兩件事都會在。

另外有兩道保險：

1. 每次合併前會自動留一份快照（保留最近 6 份）
2. 設定頁有兩顆手動覆蓋按鈕，真的對不起來時可以強制決定

## 東西還是對不起來的時候

到 **設定 → ☁️ 雲端同步** 最下面：

- **⬆ 以這台為準** — 用這台的資料完整覆蓋雲端
- **⬇ 以雲端為準** — 用雲端的資料完整覆蓋這台

兩顆都是「整份覆蓋」，被蓋掉的那份會先自動存成快照，可以在 **🕘 自動快照** 救回來。

做法：在**資料最完整的那一台**按「以這台為準」，然後到另一台按「🔄 立即同步」。

## 東西不見了怎麼救

**設定 → 🕘 自動快照** → 挑一個時間點按 **還原**。還原後會自動蓋回雲端，兩邊都會恢復。

順便建議把電腦時鐘對準：Windows 設定 → 時間與語言 → 日期與時間 → 按 **立即同步**。
（判斷邏輯已經不看時鐘了，但畫面上顯示的「幾分前」要時間準才正確。）

## 還是要備份

**雲端同步不等於備份。** 誤刪的話兩邊會一起刪掉。

建議每隔一兩週：設定頁 → **⬇ 匯出備份** → 把 JSON 存進 `backups` 資料夾。

---

# 程式更新流程

## 什麼時候需要更新

只有**我改了程式**的時候。你自己新增任務、改資料，完全不需要做這件事。

## 怎麼做（3 個動作，約 30 秒）

### ① 電腦：雙擊 `手機版-一鍵更新.bat`

黑色視窗會跑約 10 秒，出現：

```
 ==============================================
  DONE - uploaded.                         ← 上傳完成
  On your iPhone:
    swipe the app fully closed, then reopen it.
                     ↑ 手機把 App 完全滑掉再重開
 ==============================================
```

按任意鍵關掉視窗。

### ② 電腦：重開 App

關掉再點桌面捷徑。如果沒變，按 `Ctrl + Shift + R` 強制重新載入。

### ③ 手機：完全關掉 App 再重開

**不是**回主畫面就好，要**從螢幕底部往上滑並停住 → 把每日任務往上滑掉**，再點圖示重開。

完成。**你的資料不會受影響**，資料在 Supabase，程式更新只換畫面和功能。

## 完整流程長這樣

```
我改完程式  →  你雙擊「手機版-一鍵更新.bat」  →  手機滑掉重開
   ↑                    10 秒                        5 秒
（我會跟你說）
```

---

# 疑難排解

## 電腦端

**`.bat` 跑出一堆「'xxx' 不是內部或外部命令」**
舊版的 `.bat` 裡有中文，被你的命令視窗編碼弄壞了。現在的版本已經全部改成英文，直接重跑就好。如果還是這樣，代表檔案被舊版覆蓋了，跟我說。

**`.bat` 一閃就關掉**
出錯了。在檔案上按**右鍵 → 以系統管理員身分執行**再試一次。還是不行就截圖給我。

**視窗關太快，沒看到網址**
雙擊 `顯示我的網址.bat`，或登入 https://app.netlify.com 查看。網址不會遺失。

**選了 Link，但清單裡找不到我的站台**
確認 Netlify 登入的是**當初拖資料夾時用的同一個帳號**。不確定的話就選 `[+] Create & configure a new site` 建一個新的，然後手機用新網址重新加入主畫面。

**說找不到 Node.js，可是我裝了**
沒重開機。重新開機後再試。

**上傳失敗，說登入過期**
按 Windows 鍵打 `cmd` 開命令提示字元，輸入 `netlify login` 按 Enter，重新授權一次。

**Windows 跳「已保護您的電腦」**
點 **其他資訊 → 仍要執行**。（這是因為檔案沒有數位簽章，不是病毒）

## 同步

**狀態顯示「同步失敗 401」**
anon key 貼錯或不完整。回【D-5】重新整串複製，注意開頭 `eyJ` 和結尾都要包含。

**狀態顯示「同步失敗 404」**
【D-3】的 SQL 沒跑成功。回去重跑一次，確認看到 "Success"。

**狀態顯示「同步失敗：Email logins are disabled」**
【D-4】把 **Enable Email provider** 這個總開關關掉了。回去打開它，只留 **Confirm email** 是關的。存檔後等 10 秒再試。

**註冊時說「Signups not allowed for this instance」**
【D-4】的 **Allow new users to sign up** 被關掉了。打開它再試。

**註冊時說「需要先驗證信箱」**
【D-4】的 Confirm email 沒關掉。關掉後再註冊一次。

**註冊時說 User already registered**
這個信箱註冊過了，改按 **登入**。

**手機看不到電腦的資料**
檢查三件事：Project URL 一樣、anon key 一樣（最常見是複製時少字）、帳號密碼一樣。

**忘記密碼**
Supabase 後台 → **Authentication → Users** → 找到你的信箱 → 右邊選單可以直接改密碼。

## 手機

**加到主畫面後打開還是有網址列**
是用 Chrome 加的。刪掉圖示，用 Safari 重做【F-2】。

**圖示變成網頁截圖**
清 Safari 快取後重加。⚠️ **先確認同步狀態是綠色的**，不然沒同步的紀錄會不見。

**排版怪怪的**
截圖給我，我調。

## 隱私

**這個網址別人會看到嗎**
網址不會被搜尋引擎收錄。就算有人知道網址打開，也只會看到一個**空的 App**——你的紀錄存在 Supabase 你的帳號底下，有 Row Level Security 保護，別人拿不到。

上傳的只有程式檔案，`backups` 資料夾和所有說明文件都不會被傳上去。

---

# 檢查清單

電腦：

- [ ] A. 裝好 Node.js，重開機
- [ ] B. 跑 `手機版-第一次設定.bat`，選 **Link this directory to an existing site**，拿到網址
- [ ] C. 網址貼進 `site-url.txt` 存檔
- [ ] 手機滑掉重開，確認設定頁出現「☁️ 雲端同步」
- [ ] D. Supabase 建好專案、跑完 SQL、關掉 Confirm email、複製兩把鑰匙
- [ ] E. App 設定頁貼上鑰匙、註冊帳號、狀態變綠色

手機：

- [x] F. Safari 開網址 → 分享 → 加入主畫面（你已完成）
- [ ] G. 設定頁貼上一樣的鑰匙、用同一組帳密**登入**、選「用雲端這份」

驗證：

- [ ] H. 電腦→手機、手機→電腦、離線各測一次

以後：

- [ ] 我改程式 → 雙擊 `手機版-一鍵更新.bat` → 手機滑掉重開
