# 每日任務 — 使用說明

## 怎麼開啟（做一次就好）

1. 在這個資料夾找到 **`啟動每日任務.vbs`**
2. **右鍵 → 傳送到 → 桌面(建立捷徑)**
3. 桌面出現捷徑，之後雙擊它就能用

打開的視窗沒有網址列、沒有分頁，跟一般軟體一樣。想換圖示：右鍵捷徑 → 內容 → 變更圖示。

如果 .vbs 被防毒擋掉，直接雙擊 `index.html` 也一樣能用。

> **固定用同一個瀏覽器開。** 資料存在瀏覽器裡，捷徑會優先用 Chrome、沒有才用 Edge。有時用這個有時用那個，會看到兩份不同的紀錄。

---

## 六個分頁

| 分頁 | 做什麼 |
|---|---|
| 🗓️ 今日 | 今天要做的事，點一下打勾。必做的釘最上面。 |
| 📅 行事曆 | **週檢視**（排計畫、雙擊新增）／**月檢視**（看整個月）。 |
| 📋 事情 | 新增、刪除、調整重要程度，看每件事的累積成績。 |
| 📊 成績 | 做了多少、漏了多少、每件事的成績單。 |
| 🎯 目標 | 長期目標，慢慢累積進度。 |
| ⚙️ 設定 | 名字、提醒、備份、清除資料。 |

---

## 排程四種

- **🔁 每天重複** — 設定一次，之後每天自動出現，**不用每天重設**（例：運動 30 分鐘）
- **📆 每週指定** — 勾選星期幾（例：週一三五健身）
- **📅 指定日期** — 選一天（例：下週三看牙醫），有「明天／3 天後／1 週後／2 週後／1 個月後」快速鍵

### 一個重要原則：每件事只屬於它排定的那一天

行事曆上看到的永遠是**那天真正排定的事**，不會有別天的事跑進來。

- **每日／每週的任務**漏掉就是漏掉，隔天不會變成兩個。漏掉的紀錄在「成績」頁看得到
- **指定日期的事**過期沒做，會在「今日」分頁最上方的 **⚠️ 逾期未完成** 區塊提醒你，顯示「3 天前」和原本的日期。但**行事曆的今天不會出現它**，它仍然留在原本那一天
- 在逾期區勾掉之後，**行事曆上原本那天就會變成已完成**，成績也算「做到」不算「漏掉」（晚幾天補做也是做了）
- **📝 待辦** — 留在今日清單直到你完成為止

**重要**：重複性的事情只從**建立當天**開始算。今天新增「每天運動」，不會回頭把上個月算成你都沒做——成績單只會統計你真的答應自己要做的那段時間。

---

## 週檢視 — 排計畫的地方 📅

行事曆分頁打開就是週檢視。

**在那天的欄位上雙擊**（或按該欄右上角的 ＋）跳出視窗，設定三件事：

1. 要做什麼
2. **重要程度**（🔴必做 / 🟠重要 / 🔵一般 / ⚪隨手）
3. **重複方式**
   - **只有這天** — 一次性行程
   - **每週X** — 在週三新增就顯示「每週三」，選了以後每週三都會自動出現
   - **每天** — 從這天起每天出現

打完字按 `Enter` 直接存，`Esc` 取消。

其他：點事項打勾／取消；**過去的日子可以補勾**（昨天做了忘記勾，回去補一樣算，但不會亂改連續天數）；未來的日子擋著不讓先勾；那天全部做完整欄變綠。

---

## 改排程：過去的紀錄不會被改掉 ✏️

想把「游泳」從**週二、週五**改成**週一、週四**：

1. 到 **📋 事情** 分頁，按那一項右邊的 **✏️**
2. 改星期
3. 會跳出「要從什麼時候開始改？」，選 **從今天起**（預設）
4. 儲存

結果：

- **今天以前**的日子維持原本的週二、週五 —— 行事曆和成績單完全不動
- **從今天起**改用週一、週四
- 那一項會標「**改過 1 次**」，之後還可以再改，歷史一直往上疊

選 **連過去一起改** 的話，過去的日子也會用新排程重算，成績單的排程次數會跟著變。想修正一開始就設錯的排程時用這個。

---

## 拖曳調整 🖐

在**行事曆的週檢視**或**今日清單**上，可以直接拖：

- **電腦**：按住項目直接拖
- **手機**：長按一下（約 0.3 秒）再拖

兩種用法：

| 拖到哪 | 結果 |
|---|---|
| **同一天的上下** | **只調整那一天的順序**，其他天完全不受影響 |
| **拖到別的一天** | 見下表 |

> 例如「喝水」是每天都有的事，你在週一把它拖到最上面，**只有週一**變，週二到週日維持原本的順序。想每天都改就每天各拖一次。

拖到別天時：

- **📅 指定日期** → 直接改成那天
- **📆 每週** → **從今天起**改成那個星期（過去維持原樣，跟上面的編輯一樣）
- **🔁 每天** → 不能搬（本來就每天都有），會提示你去按 ✏️ 改排程
- **📝 待辦** → 變成那天的指定日期事項

> 沒拖過的日子照預設順序（重要程度）顯示。想把預設順序重新整理，到「事情」分頁按 **依重要程度重新排序**。
> 手動排過的順序只保留最近 90 天，太舊的會自動清掉。

---

## 重要程度（四級）

| | 名稱 | 意思 |
|---|---|---|
| 🔴 | **必做** | 今天絕對不能漏掉 |
| 🟠 | **重要** | 應該要完成 |
| 🔵 | **一般** | 例行事項 |
| ⚪ | **隨手** | 有空再做就好 |

- 清單**自動照這個順序排**，🔴 在最上面、⚪ 在最下面
- 顏色會出現在左側邊條、行事曆的圓點、月檢視的日期數字
- 提醒時段到的時候，有 🔴 沒完成會**單獨點名**
- 在「事情」分頁點右邊的圓點可以循環切換：🔴 → 🟠 → 🔵 → ⚪

**不會忘記的三層保護**（以「下週三看牙醫」為例）：

1. 提前 14 天，「今日」最上方出現 📌 接下來，「7 天後 · 看牙醫」每天倒數
2. 當天標黃「📅 就是今天」
3. 沒勾也**不會消失**，會移到「今日」的 **⚠️ 逾期未完成** 區塊，標「X 天前」並閃紅，直到你處理掉為止（行事曆上仍留在原本那天，不會跑到今天）

---

## 成績 📊

上面可切 **本週 / 本月 / 全部**。

- **大數字 = 完成率**，下面那條綠紅比例條就是「做到的 vs 漏掉的」
- 應該做 / 做到了 / 漏掉了 三個數字
- **每件事的成績單**：每一項排了幾次、做幾次、漏幾次、完成率百分比，**完成率低的排在最上面**——最常失守的習慣一眼看到
- 近 30 天完成率折線（100% 的日子是綠點）
- 最底下是累積：總完成數、最長連續、全清天數（三格）

> 今天不列入結算，因為還沒過完。今天的進度在最下面單獨顯示。

---

## 長期目標 🎯

給「今年看 20 本書」這種不可能一天做完的事。

**建立**：填目標名稱、總共要幾次、單位（本／次／公里…）、期限可選（有「3 個月後 / 半年後 / 一年後 / 今年年底」快速鍵）。

**記進度**：完成一次就按「＋ 記一筆進度」，可以填數量和備註（例：「第 3 本《被討厭的勇氣》」）。

**看得到過程**：

- 左邊**進度環**顯示百分比
- **折線圖**是你的實際累積，**灰色虛線**是照期限該有的理想速度——線在虛線上面就是超前，下面就是落後
- App 會直接告訴你「還有 100 天，平均每週要 1.4 本」，或沒設期限時「照目前速度大約還要 62 天」
- 下面是**時間軸**，每一筆的日期、數量、備註，按「全部紀錄」看完整清單（也可以刪錯誤的紀錄）
- 進行中的目標會在「今日」分頁底部顯示小進度條

---

## 連續天數

最上面的圓環顯示**今天的完成進度**（做完幾件／共幾件），全部做完會變綠。

- 一天內完成任何一件事，連續天數 +1
- **每連續 10 天送 1 張保護卡**（最多 2 張）。哪天真的沒做，開 App 時自動用掉一張，連續天數不歸零。用完又斷才會真的歸零

---

## 提醒

設定頁可加多個時段（預設 09:00、20:30）。有 🔴 必做還沒完成的話會被單獨點名。

用檔案直接開啟時瀏覽器會擋系統通知，這時會改用畫面上方橫幅（需要視窗開著）。放上網之後系統通知就正常。

---

## 備份（重要）

資料存在瀏覽器本機儲存。**清除瀏覽器資料 / 換電腦 / 換瀏覽器都會消失。**

設定頁 →「⬇ 匯出備份」下載 JSON，放進這個資料夾的 `backups/`。建議每週一次。還原用「⬆ 匯入備份」。

舊版備份檔也能匯入，App 會自動升級格式，連續天數、完成紀錄、長期目標都會保留（已移除的金幣、成就、經驗值、費力程度會自動清掉，舊的「非常重要」會轉成「必做」）。

---

## 想在手機上用 / 電腦手機同步

看 **`設定教學_從頭到尾.md`** — 一份文件包含所有步驟，從裝 Node.js 到手機加入主畫面到雲端同步，約 30 分鐘。

（`手機安裝教學_iPhone.md` 和 `雲端同步設定教學.md` 是同樣內容的拆解版，可以不用看。）

做完之後：

- **改資料** → 自動同步，什麼都不用做
- **我改程式** → 你雙擊一次 `手機版-一鍵更新.bat`，10 秒完成
